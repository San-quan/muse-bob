# TODO / FIXME list

(此文件可由仓库脚本自动生成；请在本地执行 grep 命令收集并粘贴)

示例命令（在仓库根目录执行）：
- Linux / Git Bash:
  grep -RIn --include="*.js" -E "TODO|FIXME" || true
- PowerShell:
  Select-String -Path *.js -Pattern "TODO|FIXME" -SimpleMatch -Recurse

将命中项粘贴到本文件中以便追踪。
